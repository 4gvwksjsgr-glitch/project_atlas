# Local Edge Function environment example — no secrets.
# Copy to supabase/functions/.env (gitignored) for local serve only.

# SUPABASE_URL=
# SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY=
# PADDLE_SANDBOX_API_KEY=
# CORS_ALLOWLIST=http://localhost:3000,https://pay.example.test
